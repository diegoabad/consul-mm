NODE_ENV=development
PORT=5000

# Database PostgreSQL (elegir una opción)
# Opción 1: URL completa (ej. Render) - si usás esto, no hace falta DB_HOST, etc.
# DATABASE_URL=postgres://user:password@host:5432/database_name
# Opción 2: Variables por separado (local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clinica_db
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Email - Elegir UNA opción:
# --- Opción A: Gmail (contraseña de aplicación, a veces falla por proxy/certificado) ---
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASSWORD=your_app_password
# EMAIL_FROM="Consultorio <your_email@gmail.com>"
# EMAIL_TLS_REJECT_UNAUTHORIZED=false

# --- Opción B: Brevo - GRATIS 300 emails/día (recomendado) ---
# Crear cuenta en https://www.brevo.com → SMTP / API → Crear clave SMTP
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=tu_email_registrado_en_brevo@ejemplo.com
EMAIL_PASSWORD=tu_clave_smtp_de_brevo
EMAIL_FROM="Consultorio <tu_email@ejemplo.com>"
EMAIL_TLS_REJECT_UNAUTHORIZED=false

# --- Opción C: Resend - GRATIS 3000 emails/mes ---
# Si definís RESEND_API_KEY, se usa Resend en lugar de SMTP (obtené la key en https://resend.com)
# Sin dominio verificado: no definas RESEND_FROM (se usa onboarding@resend.dev) o usá:
# RESEND_FROM="Consultorio <onboarding@resend.dev>"
# Con dominio verificado en https://resend.com/domains: RESEND_FROM="Consultorio <tu_email@tudominio.com>"
RESEND_API_KEY=re_xxxxxxxxxxxx
# RESEND_FROM="Consultorio <onboarding@resend.dev>"

# Logo en emails (URL pública del logo; si no se define, se usa un placeholder)
# LOGO_URL=https://tu-dominio.com/logo.png

# URL del frontend para login (usado en el email "Usuario creado")
# LOGIN_URL=https://tu-app.netlify.app/login

# Dirección y contacto para el mail de turnos (calle, número, localidad | WhatsApp | Teléfono)
EMAIL_DIRECCION=Calle y número, Localidad
EMAIL_WHATSAPP=11 1234-5678
EMAIL_TELEFONO=11 1234-5678

# Storage LOCAL
UPLOAD_DIR=./uploads
UPLOAD_MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/jpg,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document

# Logs
LOG_LEVEL=info

# Rate Limiting (por IP; 500 req/15min por defecto)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=500

# CORS (solo en producción si el front está en otro dominio, ej. Netlify)
# Orígenes permitidos separados por coma. Ej: https://tu-app.netlify.app,https://localhost:5173
# CORS_ORIGIN=https://tu-app.netlify.app
